//! `SeaORM` Entity, @generated by sea-orm-codegen 1.1.0

use sea_orm::entity::prelude::*;
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, PartialEq, DeriveEntityModel, Serialize, Deserialize)]
#[sea_orm(table_name = "points_transactions")]
pub struct Model {
    #[sea_orm(primary_key)]
    pub id: Uuid,
    pub wallet_id: Uuid,
    pub user_id: Uuid,
    pub realm_id: String,
    pub bucket_id: Uuid,
    pub r#type: String,
    pub amount: i64,
    pub balance_after: i64,
    // Type-specific balance tracking (optional, for audit trail)
    pub topup_balance_after: Option<i64>,
    pub subscription_balance_after: Option<i64>,
    // Credit type field (optional, for grant/revocation transactions)
    pub credit_type: Option<String>,
    pub description: Option<String>,
    pub client_app_id: Option<Uuid>,
    pub subscription_id: Option<Uuid>,
    pub external_ref_id: Option<String>,
    #[sea_orm(nullable)]
    pub correlation_id: Option<String>,
    pub created_at: DateTimeWithTimeZone,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {
    #[sea_orm(
        belongs_to = "super::credit_bucket::Entity",
        from = "Column::BucketId",
        to = "super::credit_bucket::Column::Id"
    )]
    CreditBucket,
    #[sea_orm(
        belongs_to = "super::points_wallet::Entity",
        from = "Column::WalletId",
        to = "super::points_wallet::Column::Id"
    )]
    PointsWallet,
}

impl Related<super::credit_bucket::Entity> for Entity {
    fn to() -> RelationDef {
        Relation::CreditBucket.def()
    }
}

impl Related<super::points_wallet::Entity> for Entity {
    fn to() -> RelationDef {
        Relation::PointsWallet.def()
    }
}

impl ActiveModelBehavior for ActiveModel {}
